import { ConfigurationError } from "../../configuration";

/**
 * Cela regroupe les erreurs liées à la validation des definitions ou des fichiers de configuration proposés
 *
 * Ajouter le service concerné par l'erreur ?
 */

export class ContainerConfigurationError extends ConfigurationError {}
