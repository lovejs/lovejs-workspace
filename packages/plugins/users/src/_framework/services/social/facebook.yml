services:
    users.social.facebook:
        module: !plugin_dir /social/services/Facebook
        tags: { tag: users.social.service, name: facebook }
        arguments:
            - !parameter users.social.facebook.options
